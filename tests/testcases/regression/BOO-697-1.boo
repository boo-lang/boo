"""
single is zero
double is zero
long is zero
int is zero
short is zero
char is zero
bool is false
"""
f as single
if f == 0: print "single is zero"

d as double
if d == 0: print "double is zero"
	
l as long
if l == 0: print "long is zero"

i as int
if i == 0: print "int is zero"
	
c as short
if c == 0: print "short is zero"

ch as char
if ch == 0: print "char is zero"

b as bool
if b == false: print "bool is false"

