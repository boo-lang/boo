// Espaço em branco não deve ser sintaticamente significante
// dentro de colchetes e parenteses
a = [
		int(prompt("primeiro: ")),
		int(prompt("segundo: "))
	]
	
for i in map(
				double,
				a
			):
	print(i)
