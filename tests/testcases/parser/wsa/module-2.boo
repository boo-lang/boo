"""
namespace Math

def square(x as int) as int:
	return (x * x)

def add(x as int, y as int) as int:
	print(string.Format('x = {0}, y = {1}', x, y))
	return (x + y)
"""
namespace Math

def square(x as int) as int:
	return (x * x)
end

def add(x as int, y as int) as int:
	print(string.Format('x = {0}, y = {1}', x, y))
	return (x + y)
end

