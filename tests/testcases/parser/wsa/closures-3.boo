"""
def adder(amount as int):
	return { value as int | return (value + amount) }

a = adder(3)
"""
def adder(amount as int):
	return { value as int | return (value + amount) }
end

a = adder(3)
