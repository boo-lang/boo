"""
def adder(amount as int):
	return callable(value as int):
		return (value + amount)


a = adder(3)
"""
def adder(amount as int):
	return callable(value as int):
		return value+amount

a = adder(3)
